// TODO: TASK-016 — Dashboard pessoal do trainer
// Responsável: [dev]
// Dados: GET /api/calls (retorna apenas calls do trainer autenticado)
// Componentes: ScoreCard, RubricBar, InsightCard (dica de coaching)
// Ver FASE1_TASK_BREAKDOWN.md para especificação completa

export default function TrainerDashboard() {
  return (
    <div>
      <p className="text-sm" style={{ color: 'var(--am-muted)' }}>
        Em desenvolvimento — TASK-016
      </p>
    </div>
  )
}
