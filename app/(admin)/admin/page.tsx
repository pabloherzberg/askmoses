// TODO: TASK-018 — Painel SaaS (Admin)
// Dados: GET /api/clients → { clients, metrics }
// Componentes: ScoreCard (métricas globais), tabela de clientes com health badge
// Ver FASE1_TASK_BREAKDOWN.md para especificação completa

export default function AdminDashboard() {
  return (
    <div>
      <p className="text-sm" style={{ color: 'var(--am-muted)' }}>
        Em desenvolvimento — TASK-018
      </p>
    </div>
  )
}
