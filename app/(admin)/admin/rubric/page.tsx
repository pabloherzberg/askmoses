// TODO: TASK-019 — Config de rubrica (Admin)
// Dados: GET /api/rubric → { sections, trend }
// Componentes: lista de RubricSections com toggle critico/opcional (visual only)
// Ver FASE1_TASK_BREAKDOWN.md para especificação completa

export default function AdminRubric() {
  return (
    <div>
      <p className="text-sm" style={{ color: 'var(--am-muted)' }}>
        Em desenvolvimento — TASK-019
      </p>
    </div>
  )
}
