// Layout herdado do grupo (trainer)/layout.tsx
// Este arquivo existe apenas para futura customização específica de /me
// Pode ser removido se não houver necessidade de layout adicional

export default function MeLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>
}
